<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="tuition"
            type="com.doan.tuvantuyensinh.domain.Tuition" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_marginVertical="3dp"
        android:background="@drawable/round_corner_bottom_nav">

        <TextView
            android:id="@+id/tuitionAmountTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#333333"
            android:text="@{`Amount: ` + tuition.soTien}" />

        <TextView
            android:id="@+id/tuitionYearTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#333333"
            android:text="@{`Year: ` + tuition.namHoc}" />
    </LinearLayout>
</layout>